kind = "web"
previewPath = "/"
title = "Quizora"
version = "1.0.0"
id = "artifacts/quizora"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20593

[services.development]
run = "pnpm --filter @workspace/quizora run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/quizora", "run", "build" ]
publicDir = "artifacts/quizora/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20593"
BASE_PATH = "/"
